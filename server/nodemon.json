{
  "watch": ["src"],
  "ignore": ["src/**/*.test.ts", "node_modules"],
  "ext": "ts,mjs,js,json,graphql",
  "exec": "rimraf dist && swc ./src -d dist && node dist/server.js",
  "legacyWatch": true
}
